name: Solidity CI/CD

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

permissions:
  contents: read

env:
  SOLC_VERSION: "0.8.20"

jobs:
  validate-naming:
    runs-on: ubuntu-latest
    name: Branch Naming Check
    steps:
      - uses: actions/checkout@v6
      - name: Validate branch naming convention
        if: github.event_name == 'pull_request'
        run: |
          BRANCH_NAME="${{ github.head_ref }}"
          if [[ "$BRANCH_NAME" =~ ^(feature|bugfix|hotfix|release|develop|main|copilot|dependabot)/ ]]; then
            echo "✓ Branch follows naming convention: $BRANCH_NAME"
          else
            echo "✗ Branch naming failed. Required: (feature|bugfix|hotfix|release|develop|main|copilot|dependabot)/description"
            exit 1
          fi

  solidity-validation:
    runs-on: ubuntu-latest
    name: Solidity Syntax Check
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install
      - name: Install Solidity compiler
        run: |
          wget https://github.com/ethereum/solidity/releases/download/v${SOLC_VERSION}/solc-static-linux
          chmod +x solc-static-linux
          sudo mv solc-static-linux /usr/local/bin/solc
          solc --version
      - name: Validate all .sol files
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          FAILED=0
          while IFS= read -r -d '' file; do
            echo "Validating: $file"
            if solc --base-path . --include-path node_modules "$file" 2>&1 | grep -i "^Error:"; then
              echo "✗ Syntax error in: $file"
              FAILED=1
            else
              echo "✓ $file syntax OK"
            fi
          done < <(find . -name "*.sol" -type f -not -path "*/node_modules/*" -not -name "*.test.sol" -not -path "*/tests/*" -print0)
          if [ "$FAILED" -eq 1 ]; then
            echo "One or more Solidity files have syntax errors."
            exit 1
          fi

  solhint-linting:
    runs-on: ubuntu-latest
    name: Solhint Linter
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
      - name: Install solhint
        run: npm install -g solhint
      - name: Run Solhint linter
        run: solhint '**/*.sol' --format stylish

  contract-compilation:
    runs-on: ubuntu-latest
    name: Contract Compilation
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
      - name: Compile contracts
        run: |
          if [ -f "hardhat.config.js" ] || [ -f "hardhat.config.ts" ]; then
            echo "Hardhat config detected — compiling with Hardhat"
            npm install
            npx hardhat compile
          else
            echo "No Hardhat config found — falling back to solc"
            wget https://github.com/ethereum/solidity/releases/download/v${SOLC_VERSION}/solc-static-linux
            chmod +x solc-static-linux
            sudo mv solc-static-linux /usr/local/bin/solc
            solc --version
            mkdir -p build
            FAILED=0
            while IFS= read -r -d '' file; do
              echo "Compiling: $file"
              if solc --optimize --bin --abi "$file" -o build/ --overwrite 2>&1; then
                echo "✓ Compiled: $file"
              else
                echo "✗ Compilation failed: $file"
                FAILED=1
              fi
            done < <(find contracts -name "*.sol" -type f -print0 2>/dev/null)
            if [ "$FAILED" -eq 1 ]; then
              echo "One or more contracts failed to compile."
              exit 1
            fi
          fi
      - name: Upload build artifacts
        if: success()
        uses: actions/upload-artifact@v7
        with:
          name: compiled-contracts
          path: |
            build/
            artifacts/
          if-no-files-found: ignore

  slither-analysis:
    runs-on: ubuntu-latest
    name: Slither Static Analysis
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install
      - name: Install Slither
        run: pip3 install slither-analyzer
      - name: Run Slither
        run: slither contracts/ --compile-force-framework hardhat --hardhat-ignore-compile || true
        # Note: `|| true` is used here only to prevent CI blocking on informational findings.
        # Review Slither output in the step logs. Remove `|| true` once all findings are triaged.
